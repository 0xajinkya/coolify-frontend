import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        primary: {
            main: "#271F30",
            contrastText: "white"
        },
    },
    typography: {
        fontFamily: "Poppins"
    }
})